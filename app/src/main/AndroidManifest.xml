<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hr.algebra.formula1">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Formula1"
        android:usesCleartextTraffic="true">
        <provider
            android:name=".SeasonContentProvider"
            android:authorities="hr.algebra.formula1.season_provider"
            android:enabled="true"
            android:exported="false" />
        <provider
            android:name=".ConstructorContentProvider"
            android:authorities="hr.algebra.formula1.constructor_provider"
            android:enabled="true"
            android:exported="false" />
        <provider
            android:name=".CircuitContentProvider"
            android:authorities="hr.algebra.formula1.circuit_provider"
            android:enabled="true"
            android:exported="false" />
        <provider
            android:name=".DriverContentProvider"
            android:authorities="hr.algebra.formula1.driver_provider"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".Formula1DataReceiver"
            android:enabled="true"
            android:exported="false" />

        <service
            android:name=".Formula1DataService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <activity android:name=".HostActivity" />
        <activity
            android:name=".SplashScreenActivity"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/SplashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>