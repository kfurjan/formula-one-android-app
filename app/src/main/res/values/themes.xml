<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Formula1" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryVariant">@color/primary_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="SplashScreenTheme" parent="Theme.Formula1">
        <item name="android:navigationBarColor">@color/white</item>
    </style>
    <style name="NavigationDrawer" parent="Theme.Formula1">
        <item name="android:listDivider">?attr/colorSecondary</item>
    </style>
    <style name="CardViewStyle" parent="Theme.Formula1">
        <item name="cardBackgroundColor">?attr/colorSecondary</item>
    </style>
</resources>